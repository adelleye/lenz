openapi: 3.0.0
info:
  title: Lenz Core Corebanking
  version: 1.0.0
  description: >
    OpenAPI source for the current Lenz Core transaction-spine HTTP surface.
    The mock endpoints are demo-only and remain gated by application config.
tags:
  - name: Corebanking
    description: Accounts, balances, transfers, ledger journals, and demo transfer-spine routes.

paths:
  /api/v1/demo/seed:
    post:
      operationId: seedDemo
      summary: Seed demo transaction-spine data
      tags: [Corebanking]
      responses:
        "200":
          description: Demo data seed result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APISeedResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/customers:
    post:
      operationId: createCustomer
      summary: Create a customer inside an institution branch
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCustomerRequest"
      responses:
        "201":
          description: Created customer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APICustomer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/customers/{customer_id}:
    get:
      operationId: getCustomer
      summary: Get a customer inside an institution
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: customer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Customer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APICustomer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/customers/{customer_id}/accounts:
    get:
      operationId: listCustomerAccounts
      summary: List accounts owned by a customer inside an institution
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: customer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Customer account list.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts:
    post:
      operationId: createAccount
      summary: Create a customer deposit account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAccountRequest"
      responses:
        "201":
          description: Created account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}:
    get:
      operationId: getAccount
      summary: Get an account inside an institution
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Account record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/balance:
    get:
      operationId: getAccountBalance
      summary: Get account ledger and available balances
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Account balance.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccountBalance"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/freeze:
    post:
      operationId: freezeAccount
      summary: Freeze an active account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountControlRequest"
      responses:
        "200":
          description: Frozen account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/unfreeze:
    post:
      operationId: unfreezeAccount
      summary: Unfreeze a frozen account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountControlRequest"
      responses:
        "200":
          description: Active account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/post-no-debit:
    post:
      operationId: activatePostNoDebit
      summary: Activate post-no-debit on an active account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountControlRequest"
      responses:
        "200":
          description: Post-no-debit account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: deactivatePostNoDebit
      summary: Deactivate post-no-debit on a post-no-debit account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountControlRequest"
      responses:
        "200":
          description: Active account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/liens:
    post:
      operationId: placeAccountLien
      summary: Place an operational lien on an account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountLienRequest"
      responses:
        "200":
          description: Active lien.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccountHold"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/liens/{lien_id}:
    delete:
      operationId: releaseAccountLien
      summary: Release an operational lien
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: lien_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseLienRequest"
      responses:
        "200":
          description: Released lien.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIAccountHold"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/accounts/{account_id}/transactions:
    get:
      operationId: listAccountTransactions
      summary: List ledger-backed account transactions
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: account_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 100
          description: Maximum number of transactions to return. Non-positive values use the 100 default, and values above 200 are capped.
        - name: before_created_at
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Return transactions created before this timestamp, ordered newest first.
        - name: before_transfer_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Optional tie-breaker cursor used with before_created_at for stable newest-first pagination.
      responses:
        "200":
          description: Account transaction history.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/APITransaction"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/internal/credits:
    post:
      operationId: createInternalCredit
      summary: Credit a customer account from an internal source account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InternalCreditRequest"
      responses:
        "200":
          description: Internal credit transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/internal/debits:
    post:
      operationId: createInternalDebit
      summary: Debit a customer account into an internal destination account
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InternalDebitRequest"
      responses:
        "200":
          description: Internal debit transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/internal/transfers:
    post:
      operationId: createInternalTransfer
      summary: Transfer money between two customer accounts in the same institution
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InternalTransferRequest"
      responses:
        "200":
          description: Internal transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/external/name-enquiry:
    post:
      operationId: externalNameEnquiry
      summary: Validate an external beneficiary account through the configured mock provider
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExternalNameEnquiryRequest"
      responses:
        "200":
          description: Provider-shaped name enquiry result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIExternalNameEnquiry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/external/transfers/outbound:
    post:
      operationId: externalOutboundTransfer
      summary: Initiate a mock external outbound transfer lifecycle
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExternalOutboundTransferRequest"
      responses:
        "200":
          description: External outbound transfer lifecycle result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIExternalOutboundTransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/external/transfers/inbound-events:
    post:
      operationId: externalInboundEvent
      summary: Ingest a mock external inbound provider event
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExternalInboundEventRequest"
      responses:
        "200":
          description: External inbound event lifecycle result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIExternalInboundEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Provider event payload conflict accepted for manual review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIExternalInboundEvent"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/transfers/mock/inbound:
    post:
      operationId: mockInboundTransfer
      summary: Record a mock inbound provider transfer event
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MockTransferRequest"
      responses:
        "200":
          description: Transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/transfers/mock/outbound:
    post:
      operationId: mockOutboundTransfer
      summary: Initiate a mock outbound provider transfer
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MockTransferRequest"
      responses:
        "200":
          description: Transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/transfers/{transfer_id}:
    get:
      operationId: getTransfer
      summary: Get a transfer by ID
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: transfer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/transfers/{transfer_id}/reverse:
    post:
      operationId: reverseTransfer
      summary: Reverse a succeeded transfer as a new ledger event
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: transfer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Reversal transfer record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/admin/ledger/journal/{journal_entry_id}:
    get:
      operationId: getJournal
      summary: Inspect a journal entry and postings
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: journal_entry_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Journal entry with postings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIJournalWithPostings"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/admin/transfers:
    get:
      operationId: listTransfers
      summary: List recent institution transfers
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
      responses:
        "200":
          description: Recent transfers.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/APITransfer"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/admin/reconciliation-items:
    get:
      operationId: listReconciliationItems
      summary: List transfers that need reconciliation or manual review
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: before_created_at
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: before_transfer_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, succeeded, failed]
        - name: provider_status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, succeeded, failed, provider_unknown]
        - name: ledger_status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, posted, no_posting, reversal_deficit]
        - name: reconciliation_status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, matched, no_action, manual_review]
      responses:
        "200":
          description: Reconciliation review items.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/APIReconciliationItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/admin/reconciliation-items/{transfer_id}:
    get:
      operationId: getReconciliationItem
      summary: Inspect one reconciliation review item
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: transfer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Reconciliation review item.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIReconciliationItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/admin/reconciliation-items/{transfer_id}/mark-reviewed:
    post:
      operationId: markReconciliationItemReviewed
      summary: Mark a reconciliation review item as reviewed
      tags: [Corebanking]
      parameters:
        - $ref: "#/components/parameters/InstitutionID"
        - name: transfer_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkReconciliationItemReviewedRequest"
      responses:
        "200":
          description: Updated reconciliation review item metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/APIReconciliationItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

components:
  parameters:
    InstitutionID:
      name: X-Institution-ID
      in: header
      required: false
      description: Optional consistency check. Protected handlers derive institution scope from the authenticated principal.
      schema:
        type: string
        format: uuid
        minLength: 1
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 1

  responses:
    Error:
      description: Error response.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Missing or invalid authenticated principal.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Authenticated principal is not allowed for the requested institution scope.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource was not found or the demo route is disabled.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Request conflicts with existing transfer state or idempotency state.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    UnprocessableEntity:
      description: Request is valid but cannot be applied.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalServerError:
      description: Unexpected server error. Includes request_id when available.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  schemas:
    ErrorResponse:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
        request_id:
          type: string
          minLength: 1

    CreateCustomerRequest:
      type: object
      required: [branch_id, customer_type]
      properties:
        branch_id:
          type: string
          format: uuid
        customer_type:
          type: string
          enum: [individual, business]
        first_name:
          type: string
          minLength: 1
        last_name:
          type: string
          minLength: 1
        business_name:
          type: string
          minLength: 1
        email:
          type: string
          format: email
          minLength: 1
        phone:
          type: string
          minLength: 1

    CreateAccountRequest:
      type: object
      required: [customer_id, account_number, name]
      properties:
        customer_id:
          type: string
          format: uuid
        account_number:
          type: string
          pattern: "^[0-9]{10}$"
          description: >
            v0.1 accepts a caller-supplied, institution-unique 10-digit
            test/account number. This is not full NUBAN issuance; true NUBAN
            generation/check-digit validation is deferred until institution
            bank-code and account-number issuance inputs are explicitly scoped.
        name:
          type: string
          minLength: 1
        product_type:
          type: string
          enum: [standard_wallet, standard_savings, standard_current]
          default: standard_wallet
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
          default: NGN
        allow_negative_balance:
          type: boolean
          default: false

    InternalCreditRequest:
      type: object
      required: [account_id, amount_minor, currency_id, idempotency_key]
      properties:
        account_id:
          type: string
          format: uuid
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
        idempotency_key:
          type: string
          minLength: 1
        narration:
          type: string
        source_account_id:
          type: string
          format: uuid
        reference:
          type: string

    InternalDebitRequest:
      type: object
      required: [account_id, amount_minor, currency_id, idempotency_key]
      properties:
        account_id:
          type: string
          format: uuid
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
        idempotency_key:
          type: string
          minLength: 1
        narration:
          type: string
        destination_account_id:
          type: string
          format: uuid
        reference:
          type: string

    InternalTransferRequest:
      type: object
      required: [source_account_id, destination_account_id, amount_minor, currency_id, idempotency_key]
      properties:
        source_account_id:
          type: string
          format: uuid
        destination_account_id:
          type: string
          format: uuid
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
        idempotency_key:
          type: string
          minLength: 1
        narration:
          type: string
        reference:
          type: string

    ExternalNameEnquiryRequest:
      type: object
      required: [destination_institution_code, account_number]
      properties:
        provider:
          type: string
          minLength: 1
          default: mock_nip
        destination_institution_code:
          type: string
          minLength: 1
        account_number:
          type: string
          pattern: "^[0-9]{10}$"
        currency_id:
          type: string
          enum: [NGN]
          default: NGN

    APIExternalNameEnquiry:
      type: object
      required:
        - provider
        - destination_institution_code
        - account_number
        - account_name
        - status
        - message
        - created_at
      properties:
        provider:
          type: string
          minLength: 1
        destination_institution_code:
          type: string
          minLength: 1
        account_number:
          type: string
          pattern: "^[0-9]{10}$"
        account_name:
          type: string
        provider_reference:
          type: string
        status:
          type: string
          enum: [found, not_found, provider_unavailable]
        message:
          type: string
          minLength: 1
        created_at:
          type: string
          format: date-time

    ExternalOutboundTransferRequest:
      type: object
      required:
        - source_account_id
        - destination_account_number
        - amount_minor
        - idempotency_key
      properties:
        source_account_id:
          type: string
          format: uuid
        destination_institution_code:
          type: string
          minLength: 1
        bank_code:
          type: string
          minLength: 1
          description: Alias for destination_institution_code.
        destination_account_number:
          type: string
          pattern: "^[0-9]{10}$"
        destination_account_name:
          type: string
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
          default: NGN
        idempotency_key:
          type: string
          minLength: 1
        provider:
          type: string
          minLength: 1
          default: mock_nip
        narration:
          type: string
        reference:
          type: string
        scenario:
          type: string
          enum: [success, failed, pending, timeout, provider_unknown]
          description: Mock-only test field for MockNIPProvider outcomes.

    APIExternalOutboundTransfer:
      type: object
      required:
        - transfer_id
        - source_account_id
        - provider
        - provider_reference
        - provider_status
        - ledger_status
        - reconciliation_status
        - status
        - amount_minor
        - currency_id
        - journal_entry_id
        - hold_id
        - created_at
      properties:
        transfer_id:
          type: string
          format: uuid
        source_account_id:
          type: string
          format: uuid
        provider:
          type: string
          minLength: 1
        provider_reference:
          type: string
        provider_status:
          type: string
          enum: [pending, succeeded, failed, provider_unknown]
        ledger_status:
          type: string
          enum: [pending, posted, no_posting, reversal_deficit]
        reconciliation_status:
          type: string
          enum: [pending, matched, no_action, manual_review]
        status:
          type: string
          enum: [pending, succeeded, failed]
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
        journal_entry_id:
          type: string
          format: uuid
          nullable: true
        hold_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time

    ExternalInboundEventRequest:
      type: object
      required:
        - provider_event_id
        - provider_reference
        - amount_minor
        - status
      properties:
        provider:
          type: string
          minLength: 1
          default: mock_nip
        provider_event_id:
          type: string
          minLength: 1
        provider_reference:
          type: string
          minLength: 1
        account_id:
          type: string
          format: uuid
        destination_account_number:
          type: string
          pattern: "^[0-9]{10}$"
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
          default: NGN
        status:
          type: string
          enum: [succeeded, pending, failed]
        sender_name:
          type: string
        sender_account_number:
          type: string
        sender_institution_code:
          type: string
        narration:
          type: string
        scenario:
          type: string
          enum: [success, pending, failed]
          description: Mock-only field for MockNIPProvider inbound event outcomes.

    APIExternalInboundEvent:
      type: object
      required:
        - transfer_id
        - provider_event_id
        - provider_reference
        - provider_status
        - ledger_status
        - reconciliation_status
        - status
        - journal_entry_id
        - message
      properties:
        transfer_id:
          type: string
          format: uuid
          nullable: true
        provider_event_id:
          type: string
          minLength: 1
        provider_reference:
          type: string
        provider_status:
          type: string
          enum: [pending, succeeded, failed, provider_unknown]
        ledger_status:
          type: string
          enum: [pending, posted, no_posting, reversal_deficit]
        reconciliation_status:
          type: string
          enum: [pending, matched, no_action, manual_review]
        status:
          type: string
          enum: [pending, succeeded, failed]
        journal_entry_id:
          type: string
          format: uuid
          nullable: true
        message:
          type: string
          minLength: 1

    AccountControlRequest:
      type: object
      required: [reference, reason]
      properties:
        reference:
          type: string
          minLength: 1
        reason:
          type: string
          minLength: 1

    AccountLienRequest:
      type: object
      required: [amount_minor, currency_id, reference, reason]
      properties:
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          enum: [NGN]
        reference:
          type: string
          minLength: 1
        reason:
          type: string
          minLength: 1

    ReleaseLienRequest:
      type: object
      required: [reference]
      properties:
        reference:
          type: string
          minLength: 1
        reason:
          type: string

    APISeedResult:
      type: object
      required: [institution, branch, customer, account, clearing_account]
      properties:
        institution:
          $ref: "#/components/schemas/APIInstitution"
        branch:
          $ref: "#/components/schemas/APIBranch"
        customer:
          $ref: "#/components/schemas/APICustomer"
        account:
          $ref: "#/components/schemas/APIAccount"
        clearing_account:
          $ref: "#/components/schemas/APIAccount"

    APIInstitution:
      type: object
      required: [id, name, short_name, code, currency_id, status, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        short_name:
          type: string
        code:
          type: string
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
        status:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    APIBranch:
      type: object
      required: [id, institution_id, code, name, status, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        code:
          type: string
        name:
          type: string
        status:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    APICustomer:
      type: object
      required: [id, institution_id, branch_id, customer_type, status, kyc_tier, bvn_status, nin_status, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        branch_id:
          type: string
          format: uuid
        customer_type:
          type: string
          enum: [individual, business]
        first_name:
          type: string
        last_name:
          type: string
        business_name:
          type: string
          nullable: true
        email:
          type: string
        phone:
          type: string
        status:
          type: string
        kyc_tier:
          type: string
        bvn_status:
          type: string
          enum: [not_collected, pending, verified, failed]
        nin_status:
          type: string
          enum: [not_collected, pending, verified, failed]
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    APIAccount:
      type: object
      required:
        - id
        - institution_id
        - account_number
        - name
        - kind
        - product_type
        - allow_negative_balance
        - currency_id
        - normal_balance
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        customer_id:
          type: string
          format: uuid
          nullable: true
        account_number:
          type: string
          description: >
            Supplied unique 10-digit account number accepted by v0.1. True
            NUBAN generation/check-digit validation is deferred.
        name:
          type: string
        kind:
          type: string
        product_type:
          type: string
        allow_negative_balance:
          type: boolean
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
        normal_balance:
          type: string
          enum: [debit, credit]
        status:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    APIAccountBalance:
      type: object
      required: [account_id, institution_id, available_minor, ledger_minor, currency_id, updated_at]
      properties:
        account_id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        available_minor:
          type: integer
          format: int64
        ledger_minor:
          type: integer
          format: int64
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
        last_journal_entry_id:
          type: string
          format: uuid
          nullable: true
        updated_at:
          type: string
          format: date-time

    APIAccountHold:
      type: object
      required: [id, institution_id, account_id, amount_minor, currency_id, status, reason, reference, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        transfer_id:
          type: string
          format: uuid
          nullable: true
        amount_minor:
          type: integer
          format: int64
        currency_id:
          type: string
        status:
          type: string
          enum: [active, released, consumed]
        reason:
          type: string
        reference:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        released_at:
          type: string
          format: date-time
          nullable: true

    APITransfer:
      type: object
      required:
        - id
        - institution_id
        - account_id
        - direction
        - status
        - provider_status
        - ledger_status
        - reconciliation_status
        - amount_minor
        - currency_id
        - idempotency_key
        - provider
        - provider_reference
        - narration
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        direction:
          type: string
          enum: [inbound, outbound, reversal]
        status:
          type: string
          enum: [pending, succeeded, failed]
        provider_status:
          type: string
          enum: [pending, succeeded, failed, provider_unknown]
        ledger_status:
          type: string
          enum: [pending, posted, no_posting, reversal_deficit]
        reconciliation_status:
          type: string
          enum: [pending, matched, no_action, manual_review]
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
        idempotency_key:
          type: string
          minLength: 1
        provider:
          type: string
          minLength: 1
        provider_reference:
          type: string
        provider_event_id:
          type: string
          nullable: true
        journal_entry_id:
          type: string
          format: uuid
          nullable: true
        reversal_of_transfer_id:
          type: string
          format: uuid
          nullable: true
        failure_reason:
          type: string
          nullable: true
        narration:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    APIReconciliationItem:
      type: object
      required:
        - transfer_id
        - institution_id
        - account_id
        - direction
        - status
        - provider
        - provider_reference
        - provider_status
        - ledger_status
        - reconciliation_status
        - amount_minor
        - currency_id
        - created_at
        - updated_at
        - review_reason
        - recommended_next_action
      properties:
        transfer_id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        direction:
          type: string
          enum: [inbound, outbound, reversal]
        status:
          type: string
          enum: [pending, succeeded, failed]
        provider:
          type: string
          minLength: 1
        provider_reference:
          type: string
        provider_event_id:
          type: string
          nullable: true
        provider_status:
          type: string
          enum: [pending, succeeded, failed, provider_unknown]
        ledger_status:
          type: string
          enum: [pending, posted, no_posting, reversal_deficit]
        reconciliation_status:
          type: string
          enum: [pending, matched, no_action, manual_review]
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
        failure_reason:
          type: string
          nullable: true
        journal_entry_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        review_reason:
          type: string
          enum:
            - manual_review
            - provider_unknown
            - reversal_deficit
            - provider_succeeded_ledger_not_posted
            - provider_failed_ledger_posted
            - stale_pending
            - none
        recommended_next_action:
          type: string
          enum:
            - requery_provider
            - inspect_journal
            - contact_provider
            - manual_customer_receivable_review
            - no_action
        review_status:
          type: string
          nullable: true
          enum: [reviewed, resolved_no_action, manual_followup_required]
        review_note:
          type: string
          nullable: true
        reviewed_at:
          type: string
          format: date-time
          nullable: true
        reviewed_by:
          type: string
          nullable: true

    MarkReconciliationItemReviewedRequest:
      type: object
      required: [resolution_note, resolution_status]
      properties:
        resolution_note:
          type: string
          minLength: 1
        resolution_status:
          type: string
          enum: [reviewed, resolved_no_action, manual_followup_required]

    MockTransferRequest:
      type: object
      required: [account_id, amount_minor]
      properties:
        institution_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
          minLength: 3
          maxLength: 3
          default: NGN
        idempotency_key:
          type: string
          minLength: 1
        provider_event_id:
          type: string
        provider_reference:
          type: string
        status:
          type: string
          enum: [pending, succeeded, failed]
          default: succeeded
        narration:
          type: string
        scenario:
          type: string
          enum: [success, pending, failed, duplicate, delayed, reversal]
        delay_seconds:
          type: integer
          format: int64
          description: Optional delay in seconds for delayed scenarios. Non-positive values use the provider default.

    APIJournalEntry:
      type: object
      required: [id, institution_id, entry_type, currency_id, narration, total_debit_minor, total_credit_minor, created_at]
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        transfer_id:
          type: string
          format: uuid
          nullable: true
        entry_type:
          type: string
          enum: [inbound, outbound, reversal]
        currency_id:
          type: string
        narration:
          type: string
        total_debit_minor:
          type: integer
          format: int64
        total_credit_minor:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time

    APIPosting:
      type: object
      required: [id, institution_id, journal_entry_id, account_id, direction, amount_minor, currency_id, created_at]
      properties:
        id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        journal_entry_id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        direction:
          type: string
          enum: [debit, credit]
        amount_minor:
          type: integer
          format: int64
          minimum: 1
        currency_id:
          type: string
        created_at:
          type: string
          format: date-time

    APIJournalWithPostings:
      type: object
      required: [journal_entry, postings, balanced]
      properties:
        journal_entry:
          $ref: "#/components/schemas/APIJournalEntry"
        postings:
          type: array
          items:
            $ref: "#/components/schemas/APIPosting"
        balanced:
          type: boolean

    APITransaction:
      type: object
      required: [id, transfer_id, account_id, institution_id, direction, status, ledger_status, provider_status, reconciliation_status, amount_minor, signed_amount_minor, currency_id, narration, provider, provider_reference, created_at]
      properties:
        id:
          type: string
        transfer_id:
          type: string
          format: uuid
        journal_entry_id:
          type: string
          format: uuid
          nullable: true
        account_id:
          type: string
          format: uuid
        institution_id:
          type: string
          format: uuid
        direction:
          type: string
          enum: [credit, debit]
        status:
          type: string
          enum: [pending, succeeded, failed]
        ledger_status:
          type: string
        provider_status:
          type: string
        reconciliation_status:
          type: string
        amount_minor:
          type: integer
          format: int64
        signed_amount_minor:
          type: integer
          format: int64
        currency_id:
          type: string
        narration:
          type: string
        counterparty_account_id:
          type: string
          format: uuid
          nullable: true
        provider:
          type: string
        provider_reference:
          type: string
        created_at:
          type: string
          format: date-time
